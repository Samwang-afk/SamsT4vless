[package]
name = "ss-gui"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[[bin]]
name = "SS-RS"
path = "src/main.rs"

[dependencies]
eframe = { version = "0.35", default-features = false, features = ["default_fonts", "glow"] }

[build-dependencies]
winresource = "0.1"
